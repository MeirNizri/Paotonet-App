<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.paotonet">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Paotonet">
        <activity android:name=".LiveStream"></activity>
        <activity android:name=".HealthStatement" />
        <activity android:name=".ParentInterface" />
        <activity android:name=".ParentLogin" />
        <activity android:name=".TeacherLogin" />
        <activity
            android:name=".ui.login.TeacherLogin"
            android:label="@string/title_activity_teacher_login" />
        <activity android:name=".TeacherInterface" />
        <activity android:name=".Messages" />
        <activity android:name=".Attendance" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>